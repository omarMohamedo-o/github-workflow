name: first workflow
on:
  push:
    branches:
      - main
jobs:
    first-build-workflow:
        runs-on: ubuntu-latest
        steps:
        - name: checkout code
          uses: actions/checkout@v2
        
        - name: Say Hello
          run: echo "Hello, World!"